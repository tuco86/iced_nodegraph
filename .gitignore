/target
/out

# Local working notes / scratch (never committed)
/plan/
/plans
.mcp.json
/FEEDBACK.md
/RELEASE_TODO.md

# WASM build artifacts (generated by wasm-pack)
/pkg
/target/wasm-build

# Local cargo config with path patches (not for CI)
/.cargo/config.toml

# cargo-mutants output (transient audit artifact)
/mutants.out

# Agent scratch files
nul

# Local render debug artifacts (checked-in doc images live in assets/)
*.png
!/assets/**/*.png

# Nsight Graphics injection overlay writes its ImGui layout to the cwd
/imgui.ini
